# Configurações de desenvolvimento local
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Segurança
SECRET_KEY=django-insecure-c6=0423zkwukyv+@*1tr^y-08p_sr2j^zty7*hsc&7d-4t528g
DEBUG=True

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1

# Banco de dados (deixe vazio para usar SQLite local)
# Para PostgreSQL local: postgresql://user:password@localhost:5432/dbname
DATABASE_URL=

# Railway Specific Variables (auto-configured by Railway)
PORT=8000
PYTHONUNBUFFERED=1
WEB_CONCURRENCY=4

# Para produção no Render/Railway, estas variáveis são configuradas automaticamente:
# DATABASE_URL=postgresql://...  (configurado pelo Render/Railway)
# SECRET_KEY=...  (gerado automaticamente)
# DEBUG=False  (padrão para produção)
# ALLOWED_HOSTS=*.railway.app,*.onrender.com  (configurado automaticamente)